import React, { Component } from 'react';
import './index.scss';

class NavBars extends Component{
    render () {
        return (
            <footer className="app-navbars">
                NavBars
            </footer>
        )
    }
}

export default NavBars;