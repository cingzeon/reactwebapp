import React, { Component } from 'react';
import './index.scss';

class Headers extends Component{
    render () {
        return (
            <footer className="app-headers">
                Headers
            </footer>
        )
    }
}

export default Headers;